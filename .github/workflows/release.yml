name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-macos:
    name: macOS (arm64)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release -p clum-mcp -p clum-cli
      - name: Package
        run: |
          mkdir -p pkg/config pkg/.opencode/skills/clum-mcp
          cp target/release/clum-mcp target/release/clum-cli pkg/
          cp config/hosts.example.yaml config/mcp-config.example.json config/rmux.conf pkg/config/
          cp .opencode/skills/clum-mcp/SKILL.md pkg/.opencode/skills/clum-mcp/
          cp README.md pkg/
          tar czf clum-macos-arm64.tar.gz -C pkg .
      - uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: clum-macos-arm64.tar.gz

  build-linux:
    name: Linux (x86_64 musl)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y musl-tools
      - run: cargo build --target x86_64-unknown-linux-musl --release -p clum-mcp -p clum-cli -p rmux-bridge
      - name: Package
        run: |
          mkdir -p pkg/config pkg/deploy pkg/.opencode/skills/clum-mcp
          cp target/x86_64-unknown-linux-musl/release/clum-mcp pkg/
          cp target/x86_64-unknown-linux-musl/release/clum-cli pkg/
          cp target/x86_64-unknown-linux-musl/release/rmux-bridge pkg/
          cp config/hosts.example.yaml config/mcp-config.example.json config/rmux.conf pkg/config/
          cp deploy/install-bridge.sh deploy/install-daemon.sh pkg/deploy/
          cp deploy/rmux-daemon.service pkg/deploy/
          cp deploy/generate-certs.sh pkg/deploy/
          cp .opencode/skills/clum-mcp/SKILL.md pkg/.opencode/skills/clum-mcp/
          cp README.md pkg/
          tar czf clum-linux-x86_64.tar.gz -C pkg .
      - uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64
          path: clum-linux-x86_64.tar.gz

  build-windows:
    name: Windows (x86_64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release -p clum-mcp -p clum-cli
      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path pkg/config, pkg/.opencode/skills/clum-mcp
          Copy-Item target/release/clum-mcp.exe, target/release/clum-cli.exe pkg/
          Copy-Item config/hosts.example.yaml, config/mcp-config.example.json, config/rmux.conf pkg/config/
          Copy-Item .opencode/skills/clum-mcp/SKILL.md pkg/.opencode/skills/clum-mcp/
          Copy-Item README.md pkg/
          Compress-Archive -Path pkg/* -DestinationPath clum-windows-x86_64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64
          path: clum-windows-x86_64.zip

  release:
    name: Create Release
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: '*'
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            clum-macos-arm64.tar.gz
            clum-linux-x86_64.tar.gz
            clum-windows-x86_64.zip
          generate_release_notes: true
